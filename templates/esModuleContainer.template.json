{
    "cwd": "{{dependencyRootDirectory}}",
    "djectLocation": "{{djectLocation}}",
    "destinationPath": "{{destinationPath}}${pathSeparator}{{destinationFileName}}",
    "modulePaths": [
        "**${pathSeparator}*.js"
    ]
}