{
    "cwd": "{{dependencyRootDirectory}}",
    "djectLocation": "{{djectLocation}}",
    "isNodeApplication": {{isNodeApplication}},
    "destinationPath": "{{destinationPath}}${pathSeparator}{{destinationFileName}}.{{languageExtension}}",
    "modulePaths": [
        "**${pathSeparator}*.{{languageExtension}}"
    ]
}
